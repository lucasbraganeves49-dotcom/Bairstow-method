# Bairstow's Method
This is the Bairstow Method implemented in Python to find all the roots of a polynomial.
To use the program you must input the polynomial degree, then its coefficients (starting from the highest exponent), and in the end the initial guesses for r and s, used to deflate the polynomial by dividing it by x² - rx - s.
# Prerequisite
Python 3.8 or above
